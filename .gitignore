# Lab managed - generated by the Lab app, not source
.claude/
AGENTS.md
CLAUDE.md
.mcp.json

# Working notes and store copy - kept on disk, never tracked
.lab/
NEXUS_MODS.md

# Build output
bin/
obj/
dist/
release/
build/
target/

# IDE
.vs/
.vscode/
.idea/
*.user
*.suo

# Tooling
.pixi/

# Installer state
.headtracking-state.json

# Game DLLs resolved from a local install. The checked-in stub source is not a DLL,
# so it stays tracked and CI keeps building without the game.
src/*/libs/*.dll
